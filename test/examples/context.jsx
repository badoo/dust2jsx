<p>{title}</p>

{text ?
<div className="header">
  <div>
    <p>{text}</p>
  </div>
</div>
 : null}

{images.map(item =>
<img src={item.img.src}/>{item.foo}</div>
)}

{{
  [IMG2]: (
    <Icon name={picture.value}/>
  )
}[picture]}

<Footer text={text}/>
